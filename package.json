{
  "name": "kingdom-of-pal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "node --env-file-if-exists=.env runtime/hello-world.mjs",
    "test": "node --test runtime/hello-world.test.mjs",
    "test:hello": "node --test runtime/hello-world.test.mjs"
  },
  "engines": {
    "node": ">=22.9.0"
  }
}
